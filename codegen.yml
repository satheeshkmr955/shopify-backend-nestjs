schema:
  - 'src/**/*.graphql'
generates:
  src/generated/graphql.ts:
    plugins:
      - typescript
      - typescript-resolvers

  src/generated/pubsub-event-map.ts:
    plugins:
      - ./plugins/codegen-pubsub.plugin.js
